import './List.scss';

const List = ({ data }) => {

    console.log(data);
    return (
        <div className="container">
            <h2>Natanael's Collection</h2>
        </div>
    );
};

export default List;